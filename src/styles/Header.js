const headerTab={
    justifyContent: "space-between", 
}

const alignRow={
    flexDirection: 'row', 
    display: 'flex'
}

const logoStyle={
    height: 100, 
    width: 100
}

const hTextStyle={
    color: '#67BCFE', 
    fontWeight: 'bold'
}

const buttonStyle={
    fontWeight: 'bold',
    width: 115,
    marginRight: '2px',
    border: '1px solid #AAAAAA',
    height: '28px'
}

export {
    headerTab,
    alignRow,
    logoStyle,
    hTextStyle,
    buttonStyle
};