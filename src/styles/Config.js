const addButton={
    marginTop: 5, 
    marginBottom: 5, 
    fontSize:13, 
    width: 140
}

const formDiv={
    flexDirection: 'row', 
                  display: 'flex', 
                  width:370
}

const contentAlign={
    justifyContent:'flex-end'
}

const fieldStyle={
    height:30
}

const textAreaStyle={
                  height:60
}

const reverseFieldStyle={
    marginLeft:78
}

const daysField={
    width:40
}

const businessDayStyle={
    fontWeight:'bold', 
    marginLeft:20
}

const saveButton={
    width:100,
    marginLeft:170,
    fontSize:13
}
const contentAlignRight={
    justifyContent:'flex-end'
}
const formDiv_1 = {
    flexDirection: 'row',
    display: 'flex',
}
export {addButton,
    fieldStyle,
    textAreaStyle,
    reverseFieldStyle,
    contentAlign,
    daysField,
    businessDayStyle,
    saveButton,
    formDiv,
    contentAlignRight,
    formDiv_1
}